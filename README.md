# Agent Platform Builder

一个功能完整的智能体平台构建网站，支持创建和管理智能体、多智能体、智能体工作流和智能体群组。

## 功能特性

### 通用功能
- ✅ 多用户支持 - 每个用户可以构建自己的资产
- ✅ 资产发布 - 支持 Public/Private 可见性设置
- ✅ 主题切换 - 支持 4 种主题：
  - 🌊 酷炫 (Cool)
  - ☀️ 明亮现代 (Modern)
  - 📚 经典 (Classic)
  - 🌸 二次元粉色 (Anime)
- ✅ 明暗模式切换

### 智能体构建
- ✅ 完整的智能体配置
  - 系统提示词
  - 模型选择（OpenAI、Anthropic、Google、Local）
  - 知识库管理
  - 工具集成（MCP Server 和 HTTP API）
  - Output Widget 配置
- ✅ 可被集成到多智能体、工作流和群组中

### 多智能体系统
- ✅ 自动相关智能体回复
- ✅ Mention 功能 - 使用 @智能体名称 指定回复
- ✅ 灵活的成员管理

### 智能体 Workflow
- ✅ 多步骤支持
- ✅ 步骤内并发执行
- ✅ 执行条件设置
- ✅ 可视化步骤编辑

### 智能体群组
- ✅ 支持智能体和多智能体加入
- ✅ 智能停止机制
  - 质量分数检测
  - 重复内容检测
  - 最大轮数限制
- ✅ 成员启用/禁用控制

## 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **状态管理**: Zustand
- **UI 组件**: 自定义组件库
- **数据持久化**: LocalStorage (Zustand Persist)

## 项目结构

```
src/
├── app/                    # Next.js App Router 页面
│   ├── agents/            # 智能体页面
│   ├── multi-agents/      # 多智能体页面
│   ├── workflows/         # 工作流页面
│   ├── groups/            # 群组页面
│   └── dashboard/         # 仪表板页面
├── components/            # React 组件
│   ├── ui/               # 基础 UI 组件
│   ├── Layout/           # 布局组件
│   ├── Agent/            # 智能体组件
│   ├── MultiAgent/       # 多智能体组件
│   ├── Workflow/         # 工作流组件
│   └── Group/            # 群组组件
├── store/                # Zustand 状态管理
│   ├── userStore.ts      # 用户状态
│   ├── themeStore.ts     # 主题状态
│   ├── agentStore.ts     # 智能体状态
│   ├── multiAgentStore.ts # 多智能体状态
│   ├── workflowStore.ts  # 工作流状态
│   └── groupStore.ts     # 群组状态
├── types/                # TypeScript 类型定义
├── lib/                  # 工具函数
│   ├── utils.ts          # 通用工具
│   └── themes.ts         # 主题配置
└── styles/               # 全局样式

```

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000)

### 构建生产版本

```bash
npm run build
npm start
```

### 类型检查

```bash
npm run type-check
```

## 使用指南

### 1. 用户登录/注册

- 访问首页，输入用户名和邮箱
- 系统会自动创建或登录账号

### 2. 创建智能体

1. 进入 "智能体" 页面
2. 点击 "创建智能体"
3. 填写基本信息、配置模型、添加知识库
4. 选择可见性（私有/公开）
5. 保存

### 3. 创建多智能体

1. 确保已创建至少一个智能体
2. 进入 "多智能体" 页面
3. 点击 "创建多智能体"
4. 选择要包含的智能体
5. 配置自动回复和 Mention 功能
6. 保存

### 4. 创建工作流

1. 进入 "Workflow" 页面
2. 点击 "创建 Workflow"
3. 添加步骤，为每个步骤选择智能体
4. 设置步骤是否并发执行
5. 可选：设置执行条件
6. 保存

### 5. 创建群组

1. 进入 "群组" 页面
2. 点击 "创建群组"
3. 选择智能体和多智能体成员
4. 配置停止条件（质量分数、重复阈值）
5. 设置最大对话轮数
6. 保存

### 6. 主题切换

- 点击右上角主题下拉菜单
- 选择喜欢的主题
- 点击月亮/太阳图标切换明暗模式

## 数据模型

### Agent (智能体)
```typescript
{
  id: string;
  name: string;
  description: string;
  prompt: string;
  model: {
    provider: 'openai' | 'anthropic' | 'google' | 'local';
    modelName: string;
    temperature: number;
    maxTokens: number;
  };
  knowledgeBase?: KnowledgeBase[];
  tools?: Tool[];
  visibility: 'public' | 'private';
}
```

### MultiAgent (多智能体)
```typescript
{
  id: string;
  name: string;
  config: {
    autoReply: boolean;
    mentionEnabled: boolean;
    agents: string[];  // Agent IDs
  };
  visibility: 'public' | 'private';
}
```

### AgentWorkflow (智能体工作流)
```typescript
{
  id: string;
  name: string;
  steps: Array<{
    name: string;
    agentIds: string[];
    concurrent: boolean;
    condition?: string;
  }>;
  visibility: 'public' | 'private';
}
```

### AgentGroup (智能体群组)
```typescript
{
  id: string;
  name: string;
  members: Array<{
    id: string;
    type: 'agent' | 'multiAgent';
    enabled: boolean;
  }>;
  stopConditions: {
    enabled: boolean;
    minQualityScore: number;
    repetitionThreshold: number;
  };
  maxRounds: number;
  visibility: 'public' | 'private';
}
```

## 主题系统

所有主题支持明暗两种模式，配色方案完全自定义：

- **酷炫主题**: 蓝色调，现代感强
- **明亮现代**: 绿色调，清新明快
- **经典主题**: 灰色调，简洁专业
- **二次元粉色**: 粉色调，可爱梦幻

## 开发说明

### 添加新主题

在 `src/lib/themes.ts` 中添加新主题配置：

```typescript
export const themes: Record<ThemeType, Theme> = {
  // ... 现有主题
  newTheme: {
    name: '新主题',
    type: 'newTheme',
    cssVars: {
      light: { /* CSS 变量 */ },
      dark: { /* CSS 变量 */ },
    },
  },
};
```

### 扩展数据模型

在 `src/types/index.ts` 中添加或修改类型定义，然后更新对应的 Store。

## License

MIT

## 作者

Agent Platform Builder Team
